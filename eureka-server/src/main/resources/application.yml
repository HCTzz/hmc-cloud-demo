server:
  port: 8761

eureka:
  instance:
    prefer-ip-address: true
  client:
    #eureka.client.registerWithEureka:false fetchRegistry:false来表明自己是一个eureka server..
    registerWithEureka: false
    fetchRegistry: false
#    serviceUrl:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
